Chronogram + Cell sampling + Peak to Average

$$\newcommand{\cI}{\mathcal I}$$

- Dynamic Data Structure problem 
- Static data structure problem with a pre-intialized memory and cache
- Static Data structure problem to a One-sided communication protocol with advantage $exp(- t_q\log^2 t_uw/\sqrt{\log n})$ . 
- One-way protocol for such a problem can have advantage  $\leq exp(- \log n)$.  


Weighted communication game: 

Advantage:  

$Adv(\cP, \cD_Q, \cD_I) = \E_{Q,M_A}[{\large|}\E[\cP(Q,I)|M_A]{\large|}]$   


>[!def] $\epsilon$ close to $K$-wise independence 
Let $X = X_1 , \dots , X_n$ be a random variable. 
>$X$ is $\epsilon$-close to a $K$-wise independence, if $\exists$ a random variable $Y$ such that 
>- $\Delta(X,Y) \le \epsilon$
>- $Y$ is a $K$-wise independent random variable. 


>[!claim] Claim: 
If under a  distribution $D_{\cI}$ on the columns of the communication matrix form an $\epsilon$-close to $K$-wise independent random variable, then the one-way discrepancy is  


Distance for a K-wise independent distribution is defined as $\sum_{S \in [n], |S| = K} |\E[\prod_{i \in S} X_i]|$

If this distance is at most $\Gamma$, then for every subset of coordinates $S$ of size at most $K$, $X_S$ is $\epsilon$ close to uniform. 

$\E_{I}[\langle P_I, \chi \rangle^r] \leq \Gamma$

$\langle P_I,\chi\rangle = \sum_{q \in \cQ} P(Q,I)\cdot\chi(q)$

If the distribution on the columns is $\Gamma$  close to$K$-wise independent distribution, 
Let $\epsilon$ be the advantage obtained by a protocol which communicates m bits in the one way communcation. 

$\epsilon = |\cQ|^{-1}\E_I[\langle P_I,\chi_{m_A}\rangle]$ for the $\chi$ which is the sign vector of the rectangle $m_A$. 

we know that $\E_I[\langle P_I,\chi\rangle ^r]$ is upper-bounded by $\Gamma_r$, the distance from $r$-wise independence. 

$\Gamma = \sum_{S \subseteq |Q|, |S| = r} |\E[\prod_{i \in S} X_i]|$

$\epsilon \leq |Q|^{-8/9} \Gamma_r^{1/r}2^{m/r}$

$\Gamma \leq |\cQ|^{-1/2}$

What role does tensoring play in all this whole business, 

When you tensor how does that effect $k$ wise independence. 

