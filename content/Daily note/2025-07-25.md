Plan for this section: 
the lower-bound on various DS problems is by reduction from path-parity on multiple butterflys. 
- Explain the path-XOR on multiple butterflys. 
- Then explain how what the frame of path-XOR on multiple butterfly graph is 
- then explain how the chronogram method reduces this to a one-way protocol for path-XOR on a SINGLE butterfly. 
- Then explain how cell-sampling allows the one-way protocol to succeed with non-trivial advantage. 
- Explain that, up to this point, nothing depends on the table and everything depends only on the frame.
- Then state the required claim about path-XOR which shows that no one-way protocol can succeed. 
- Then show how if the original function was random, the resulting SINGLE butterfly instance is a random sub-table of the original, or something like that.
- Then explain that this sub-instance has high discrepancy, or something like that.
- As for the graph connectivity, the plan same as previous, except we now use path bitwise-XOR of b-bit strings instead of XOR (of 1-bit strings).